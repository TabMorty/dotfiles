# Starship configuration

add_newline = false
command_timeout = 2000

format = "$git_branch$git_status$python$character"

[git_branch]
format = "[$branch]($style) "
symbol = ""

[git_status]
format = "[$all_status$ahead_behind]($style) "
conflicted = "x"
ahead = ">"
behind = "<"
diverged = "<>"
untracked = "?"
stashed = "s"
modified = "m"
staged = "+"
renamed = "r"
deleted = "d"

[python]
format = "[$version]($style) "
symbol = ""
detect_extensions = []
detect_folders = []
detect_files = ["pyproject.toml", "requirements.txt", "requirements.in", "Pipfile", ".python-version"]

[aws]
disabled = false

[gcloud]
disabled = true

[ruby]
disabled = true
